import { Flex } from '@chakra-ui/react'

//This is rendered very simply in the figma, just position it at the bottom of the stack layout

const Footer = (props) => <Flex as="footer" py="8rem" {...props} />

export default Footer